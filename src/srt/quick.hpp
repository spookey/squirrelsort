#ifndef _quick_hpp_
#define _quick_hpp_

#include <algorithm>
#include <cmath>
#include <cstdint>

void quickSort(int16_t* point, uint32_t lpos, uint32_t rpos);
void quickSort(int16_t* point, uint32_t len);

#endif
