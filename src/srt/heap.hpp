#ifndef _heap_hpp_
#define _heap_hpp_

#include <algorithm>
#include <cmath>
#include <cstdint>

void heapify(int16_t* point, uint32_t len, uint32_t pos);
void heapSort(int16_t* point, uint32_t len);

#endif
